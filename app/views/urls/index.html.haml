%h1 All URLs

%table
  %tr
    %th Original URL
    %th Short URL
  - @urls.each do |url|
    %tr
      %td= url.long_url
      %td= link_to url_url(url), url_url(url)
      %td= link_to "Info", [:info, url]
